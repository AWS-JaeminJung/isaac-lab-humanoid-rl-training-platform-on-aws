# KAI Scheduler Preemption Demo
#
# Demo flow:
#   1. Submit low-priority GPU holder:  POST /api/pool/eks-gpu-robotics/workflow  (h1-kai-low-priority below)
#   2. Wait for it to start running and occupy GPUs
#   3. Submit this high-priority training: POST /api/pool/eks-gpu-robotics/workflow  (h1-kai-preempt)
#   4. KAI Scheduler preempts the low-priority job to free GPUs for the high-priority training
#
# Prerequisites:
#   - KAI Scheduler installed (helm install kai-scheduler nvidia/kai-scheduler)
#   - PriorityClasses created:
#       kubectl apply -f - <<EOF
#       apiVersion: scheduling.k8s.io/v1
#       kind: PriorityClass
#       metadata:
#         name: high-priority
#       value: 1000000
#       globalDefault: false
#       description: "High priority for preemption demos"
#       ---
#       apiVersion: scheduling.k8s.io/v1
#       kind: PriorityClass
#       metadata:
#         name: low-priority
#       value: 100
#       globalDefault: false
#       description: "Low priority, can be preempted"
#       EOF
#   - OSMO pool pod_template configured with priorityClassName
#     (or use kai-scheduler annotations via pool platform labels)

# === HIGH PRIORITY: H1 Training (preempts low-priority jobs) ===
workflow:
  name: h1-kai-preempt
  tasks:
  - name: train-h1
    image: 031273582910.dkr.ecr.us-east-1.amazonaws.com/isaac-lab-training:latest
    environment:
      REQ_GPU: "2"
    command:
    - bash
    - -c
    - |
      cd /workspace/isaaclab
      mkdir -p /isaac-sim/kit/data/Kit/Isaac-Sim/5.0/pip3-envs/default
      export WARP_LOG_LEVEL=error

      echo "============================================================"
      echo "[KAI Preempt] High-Priority H1 Training"
      echo "  This job should preempt lower-priority GPU workloads"
      echo "============================================================"
      nvidia-smi -L

      /isaac-sim/python.sh -c "
      import os, sys, subprocess, re, time, mlflow

      TRACKING_URI = 'http://mlflow-tracking.mlflow.svc/mlflow'
      WORKFLOW_NAME = 'h1-kai-preempt'
      WORKFLOW_ID = os.environ.get('OSMO_WORKFLOW_ID', WORKFLOW_NAME)
      TASK = 'Isaac-Velocity-Rough-H1-v0'
      NUM_ENVS = 4096
      MAX_ITERATIONS = 10

      print('=' * 60)
      print('[STEP 1/4] Environment Setup (HIGH PRIORITY)')
      print('=' * 60)
      t0 = time.time()
      print(f'  Workflow: {WORKFLOW_ID}')
      print(f'  Task: {TASK}, Envs: {NUM_ENVS}, Iters: {MAX_ITERATIONS}')
      print(f'  Config: 2 GPUs, HIGH PRIORITY (KAI Preempt)')
      mlflow.set_tracking_uri(TRACKING_URI)
      mlflow.set_experiment(WORKFLOW_NAME)

      with mlflow.start_run(run_name=WORKFLOW_ID):
          mlflow.log_params({
              'task': TASK, 'num_envs': NUM_ENVS,
              'max_iterations': MAX_ITERATIONS,
              'algorithm': 'PPO', 'framework': 'rsl_rl',
              'num_gpus': 2, 'distributed': True,
              'priority': 'HIGH', 'scheduler': 'kai',
              'workflow_id': WORKFLOW_ID,
          })
          mlflow.set_tag('priority', 'HIGH')
          mlflow.set_tag('scheduler', 'kai')

          print('=' * 60)
          print('[STEP 2/4] Training (2 GPUs, torchrun)')
          print('=' * 60)
          t1 = time.time()

          cmd = ['/isaac-sim/python.sh', '-m', 'torch.distributed.run',
              '--standalone', '--nproc_per_node=2',
              'scripts/reinforcement_learning/rsl_rl/train.py',
              f'--task={TASK}', f'--num_envs={NUM_ENVS}',
              f'--max_iterations={MAX_ITERATIONS}', '--headless', '--distributed']

          process = subprocess.Popen(cmd, stdout=subprocess.PIPE,
              stderr=subprocess.STDOUT, universal_newlines=True, bufsize=1)

          iteration = 0
          for line in process.stdout:
              print(line, end='')
              if 'Mean reward' in line:
                  m = re.search(r'Mean reward:\s*([-\d.]+)', line)
                  if m: mlflow.log_metric('mean_reward', float(m.group(1)), step=iteration)
              if 'Mean episode length' in line:
                  m = re.search(r'Mean episode length:\s*([-\d.]+)', line)
                  if m: mlflow.log_metric('mean_episode_length', float(m.group(1)), step=iteration)
              if 'Iteration time' in line:
                  m = re.search(r'Iteration time:\s*([\d.]+)', line)
                  if m:
                      mlflow.log_metric('iteration_time_sec', float(m.group(1)), step=iteration)
                      iteration += 1

          process.wait()
          mlflow.log_metric('exit_code', process.returncode)
          mlflow.log_metric('training_time_sec', time.time() - t1)

          print('=' * 60)
          print('[STEP 3/4] MLflow Upload')
          print('=' * 60)

          import torch, shutil, glob, tempfile
          log_dir = 'logs/rsl_rl/h1_rough'
          if os.path.exists(log_dir):
              runs = sorted(os.listdir(log_dir))
              if runs:
                  model_dir = os.path.join(log_dir, runs[-1])
                  files = os.listdir(model_dir)
                  print(f'  Found {len(files)} files in {model_dir}')
                  pt_files = sorted(glob.glob(os.path.join(model_dir, 'model_*.pt')))
                  if pt_files:
                      best_ckpt = pt_files[-1]
                      print(f'  Uploading: {best_ckpt}')
                      staging = tempfile.mkdtemp()
                      shutil.copy2(best_ckpt, staging)
                      with open(os.path.join(staging, 'MLmodel'), 'w') as mf:
                          mf.write(f'artifact_path: model\nflavors:\n  pytorch:\n    model_data: {os.path.basename(best_ckpt)}\n    pytorch_version: {torch.__version__}\nmodel_size_bytes: {os.path.getsize(best_ckpt)}\n')
                      mlflow.log_artifacts(staging, artifact_path='model')
                  else:
                      mlflow.log_artifacts(model_dir, artifact_path='model')
                  for f in files:
                      fpath = os.path.join(model_dir, f)
                      if not f.endswith('.pt') and os.path.isfile(fpath):
                          mlflow.log_artifact(fpath, artifact_path='model/extras')
                  osmo_out = '/osmo/data/output/model'
                  os.makedirs(osmo_out, exist_ok=True)
                  for f in files:
                      src, dst = os.path.join(model_dir, f), os.path.join(osmo_out, f)
                      if os.path.isdir(src): shutil.copytree(src, dst, dirs_exist_ok=True)
                      else: shutil.copy2(src, dst)
                  try:
                      mlflow.register_model(f'runs:/{mlflow.active_run().info.run_id}/model', WORKFLOW_NAME)
                      print(f'  Model registered: {WORKFLOW_NAME}')
                  except Exception as e:
                      print(f'  Registration skipped: {e}')
          else:
              print(f'  WARNING: {log_dir} not found')

          print('=' * 60)
          print('[STEP 4/4] Done')
          print('=' * 60)
          total = time.time() - t0
          mlflow.log_metric('total_duration_sec', total)
          print(f'  MLflow run: {mlflow.active_run().info.run_id}')
          print(f'  Duration: {total:.1f}s')
      "
      exit 0
    resources:
      cpu: 8
      memory: 32Gi
      gpu: 2
      storage: 50Gi

# === LOW PRIORITY: GPU Holder (submit this first to occupy GPUs) ===
# Save as separate file or submit via:
#   cat <<'EOF' | curl -X POST /api/pool/eks-gpu-robotics/workflow \
#     -H "Authorization: Bearer $TOKEN" -H "Content-Type: application/json" \
#     -d "$(python3 -c 'import json,sys; print(json.dumps({\"file\": sys.stdin.read()}))')"
#   workflow:
#     name: h1-kai-low-priority
#     priority: LOW
#     tasks:
#     - name: gpu-hold
#       image: 031273582910.dkr.ecr.us-east-1.amazonaws.com/isaac-lab-training:latest
#       command:
#       - bash
#       - -c
#       - |
#         echo "Low-priority GPU holder - occupying 4 GPUs for 30 minutes"
#         nvidia-smi -L
#         for i in $(seq 1 1800); do
#           if [ $((i % 300)) -eq 0 ]; then echo "Running... $((i/60))min elapsed"; fi
#           sleep 1
#         done
#       resources:
#         cpu: 4
#         memory: 16Gi
#         gpu: 4
#         storage: 10Gi
# EOF
